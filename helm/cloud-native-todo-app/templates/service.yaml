apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-native-todo-app.fullname" . }}
  labels:
    {{- include "cloud-native-todo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-native-todo-app.selectorLabels" . | nindent 4 }}
